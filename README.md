# Basic TODO Website using React
1. It will have the following features:
   - Add a new task
   - Mark a task as completed
   - Delete a task
2. The data will be stored in the local storage of the browser, so that it persists even after the page is refreshed.